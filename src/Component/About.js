import React from "react";

const About = ()=>{
    return(
        <div className="about">
            <h1>About</h1>
            <p>This is the React.js application that uses the technology like <b> React.js, Node.js, Express.js & MongoDB</b></p>
            <p>We can created the database that can store the information of an User & Jobs, The user firstly need to register her self then he can login and after user can apply the jobs on our portal</p>
            <p>In this application we can creating the Jobs backend database management system, In this we can add job to database, update job as per need in database & delete job after expiry in database meance all CRUD Operation we can perform here. Also here is search option that can search the item/content in the available database.</p>
        </div>
    )
}

export default About;